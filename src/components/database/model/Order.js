class Order {
    constructor(id, nom, quantity, current_quantity, needed_quantity) {
        this.id = id;
        this.nom = nom;
        this.quantity = quantity;
        this.current_quantity = current_quantity;
        this.needed_quantity = needed_quantity;
    }
}
export default Order