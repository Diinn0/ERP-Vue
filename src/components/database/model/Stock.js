class Stock {
    constructor(id, nom, current_quantity, needed_quantity) {
        this.id = id;
        this.nom = nom;
        this.current_quantity = current_quantity;
        this.needed_quantity = needed_quantity;
    }
}
export default Stock